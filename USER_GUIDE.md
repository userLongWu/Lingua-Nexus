# Lingua Nexus 使用指南

这是无需账号的本地桌面学习 MVP。通过 `npm run tauri dev` 打开应用；安装、打包与运行时配置见 [README](./README.md)。AI 是可选功能，未配置时仍能手动建卡、导入、编辑和复习。

## 1. 配置 AI（可选）

在项目根目录将 `.env.example` 复制为 `.env.local`，填写：

| 变量 | 含义 |
| --- | --- |
| `AI_BASE_URL` | OpenAI 兼容 API 基础地址，例如 `https://api.openai.com/v1`，不要带 `/chat/completions` |
| `AI_MODEL` | 提供方支持且你的账号有权限使用的模型 |
| `AI_API_KEY` | 你自己的密钥 |

保存后重启 `npm run tauri dev`，到 **Data → AI translation** 查看状态。终端中的同名环境变量优先于 `.env.local`。配置状态只检查参数，不验证密钥权限或连接；是否可用以实际翻译结果为准。

HTTPS 地址适用于线上服务；HTTP 仅允许 `localhost`、`127.0.0.1`、`::1`。不要在基础地址中放账号、密码或查询参数。密钥只供桌面后端使用，不显示在前端、不存入数据库或备份。`.env.local` 已被 Git 忽略。

打包后的应用同样需要**进程运行时环境变量**。构建时的密钥不会嵌入安装包，Finder 双击也不会自动加载项目的 `.env.local`；应从已设置变量的终端或进程启动配置运行可执行文件。

## 2. 添加第一张卡

进入 **Import**，填写手动表单：

| 字段 | 示例 | 说明 |
| --- | --- | --- |
| Original text | I'm gonna head out. | 必填，复习时首先展示 |
| Translation | 我要走了。 | 翻面时展示 |
| Source title | 日常口语 | 可选来源 |
| Tags | 口语, 日常 | 逗号分隔 |

译文可以手动填写，也可以按下列步骤请求 AI：

1. 点击 **Translate with AI**。只会将当前原文发给配置的提供方。
2. 等待并检查中文译文预览。此时尚未改动译文输入框或保存卡片。
3. 点击 **Apply translation** 将结果填入译文输入框，仍可自行修改。
4. 点击 **Create card**。出现成功提示后才表示已保存。

请求期间如果修改了原文或译文，旧结果会失效，避免覆盖刚刚输入的内容。错误不会清空原文；处理原因后可再次点击翻译。单次请求最多等待 30 秒，不自动重试；输入最多 20,000 UTF-8 字节。

## 3. 批量导入

在字幕区域填写来源标题，粘贴 SRT 或纯文本。预览会显示总卡片数和前 5 条，确认后点击 **Import cards**。

- SRT：每个字幕片段一张卡，同一片段的多行正文合并。
- 纯文本：每个非空行一张卡，一行中的多个句子保留在一起。
- 缩写、小数、省略号和数字对白会保留。
- 只有时间轴而没有正文时不能导入。
- 批量写入使用事务，失败不会留下部分导入记录。

导入不自动生成译文。稍后可在 **Cards → Edit** 手动补充或使用 AI。

## 4. 查询英文单词

在词典区域输入一个英文单词并查询，检查释义后点击 **Create card**。保存成功后按钮显示已保存状态。修改查询词会清除旧结果，避免保存过期查询。

词典使用独立的在线 Free Dictionary API，无需 AI 密钥；提供英文释义和可能存在的音标，不是中文翻译服务。复习词典卡时先显示单词，翻面才显示释义及已填写的译文。

## 5. 查看、编辑与删除

进入 **Cards**，搜索或按来源筛选，选择卡片后点击 **Edit**：

- 可修改原文、译文、标签、来源及下次复习日期。
- AI 操作同样是 **Translate with AI → 检查预览 → Apply translation → Save changes**。只应用而不保存不会写入数据库。
- 切换卡片后，旧卡的异步历史或保存结果不会替换当前选择。
- 删除需要确认；卡片及其全部复习历史会一起删除。

## 6. 复习与统计

进入 **Review**。新卡立即进入到期队列；先回想，再点 **Show answer**：

| 评分 | 含义 |
| --- | --- |
| Again | 没想起来 |
| Hard | 想起来了，但有困难 |
| Good | 正常想起来了 |
| Easy | 很容易想起来 |

评分保存成功后进入下一张；复习历史和下次日程作为一次事务保存，失败时可以重试。

**Dashboard** 显示总卡片数、到期数、**Reviews today** 和 **Last 7 days**。每完成一次评分记一次复习操作，按设备的本地日统计，包括今天及前 6 天；没有记录的日期显示 0。次数不代表唯一卡片数、正确率或连续学习天数。

## 7. 备份与恢复

### 导出

进入 **Data → Export backup**。成功提示会给出完整路径，文件在系统 **Downloads** 目录，名称包含时间和唯一标识，不覆盖之前的备份。

版本化 JSON 保存所有卡片字段及复习历史，不含 AI 配置或密钥，不复制媒体路径指向的外部文件。当前限制为 20 MiB、50,000 张卡和 200,000 条复习记录。

### 合并恢复

在 **Restore a backup** 选择 JSON 文件。应用先校验版本、字段、日期、日程数值及历史关联，再事务写入；出错时整次恢复不会留下部分数据。

| 备份与本地的关系 | 处理 |
| --- | --- |
| 本地不存在的 ID | 新增 |
| 相同 ID、内容相同 | 跳过，避免重复 |
| 相同卡片 ID、内容不同 | 保留本地卡片，并跳过该备份卡的关联历史 |
| 相同复习记录 ID、内容不同 | 保留本地记录，报告冲突 |

恢复不会清空当前库，也不会用备份覆盖较新的本地记录。完成后检查 **added / skipped / conflicts** 数量，再打开 Dashboard 或 Cards 查看结果。重复恢复同一文件不会重复创建历史。

支持的复习日程范围为 ease 1.3–10、重复次数 0–10,000、间隔 0–36,500 天；越界备份会整体拒绝。正常评分在极端边界封顶，保证之后仍能导出和恢复。

## 数据位置与限制

macOS 默认数据库：

```text
~/Library/Application Support/com.linguanexus.desktop/lingua-nexus.sqlite3
```

其他系统使用 Tauri 对应的应用本地数据目录。同一应用标识和目录下重启会保留记录。如需复制数据库文件，请先退出应用；日常推荐使用 Data 页的 JSON 备份。

没有账号、云同步、AI 写作区或知识图谱。真实 AI 会发送当前原文给你配置的提供方，词典会发送查询单词；卡片库不会自动上传。当前 AI 验证使用本机 HTTP fixture，**尚未用真实提供方密钥完成线上验证**。

## 常见问题

**普通浏览器里数据或 AI 加载失败**：请使用 `npm run tauri dev` 打开的桌面窗口。网页没有 Tauri 后端接口。

**AI 显示 Not configured**：补齐 `.env.local` 的三个变量后重启；检查终端是否设置了同名变量。打包应用需要运行时注入环境，不能依赖构建时配置。

**AI 授权、配额或连接错误**：分别检查密钥和模型权限、提供方额度、基础地址和网络。无效返回表示提供方未返回要求的 JSON 译文，可重新请求；超时后原文仍保留。没有模拟答案作为后备。

**翻面没有译文**：手动或字幕卡需要先填写译文。AI 预览还需应用并保存；词典卡会展示已有英文释义。

**没有卡片到期**：查看 Cards 中的下次日期。演示新一轮可创建新卡，不必删除现有记录。

**恢复后看不到备份里的旧内容**：检查冲突数量。相同 ID 内容不一致时按设计保留本地数据，恢复不会强制覆盖。

**词典查询失败**：检查网络或稍后重试。词典与 AI 是两个独立服务，已有本地学习流程仍可使用。
