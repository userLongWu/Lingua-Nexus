# Lingua Nexus 使用指南

这是本地桌面 Demo，当前可完成“添加卡片 → 查看与编辑 → 复习 → 下次再复习”。请通过 `npm run tauri dev` 打开桌面应用，完整环境说明见 [README](./README.md)。

## 1. 添加第一张卡

进入 **Import**，在手动表单中填写：

| 字段 | 示例 | 说明 |
| --- | --- | --- |
| Original text | I'm gonna head out. | 必填，复习时首先展示 |
| Translation | 我要走了。 | 翻面时展示，建议填写 |
| Source title | 日常口语 | 可选的内容来源 |
| Tags | 口语, 日常 | 用逗号分隔 |

点击 **Create card**。成功提示出现后，内容才已保存；失败时按页面提示处理后重试。

## 2. 批量导入

在字幕导入区域填写来源标题，粘贴 SRT 或纯文本。先检查预览，再点击导入。

- **SRT**：每个字幕片段生成一张卡，同一片段的多行正文合并。
- **纯文本**：每个非空行生成一张卡，一行内的多个句子保留在同一张卡。
- 缩写、小数和省略号不会被拆开，纯数字对白也会保留。
- 导入不会自动生成译文，可以稍后在 **Cards** 中补充。

## 3. 查询英文单词

在词典区域输入英文单词并查询。查看释义后点击 **Create card**。保存成功后，该结果会显示已保存状态。

词典需要网络，返回的是英文释义和可能存在的音标、例句。它不是中英翻译服务。发起新查询时，以新结果为准；查询失败可以重试，手动建卡仍可使用。

## 4. 查看、编辑与删除

进入 **Cards**，用搜索框或来源筛选找到卡片，然后打开详情：

- 可以修改原文、译文、标签和下次复习日期。
- 保存后会更新列表。
- 复习历史属于当前选中的卡片。
- 删除前需要确认；删除卡片也会删除它的复习历史。

## 5. 开始复习

进入 **Review**。新建卡片可以立即进入待复习队列。

1. 先看原文；词典卡先看单词。
2. 回想后点击 **Show answer**。词典卡显示释义，其他卡片显示你填写的译文。
3. 选择评分：

| 按钮 | 含义 |
| --- | --- |
| Again | 没想起来 |
| Hard | 想起来了，但有困难 |
| Good | 正常想起来了 |
| Easy | 很容易想起来 |

评分保存成功后进入下一张，系统同时更新复习历史和下次日期。完成后显示本次复习数量。

**Dashboard** 目前显示总卡片数和到期数，没有连续学习天数或正确率统计。

## 常见问题

**打开网页后数据加载失败？**

请使用 `npm run tauri dev` 打开的桌面窗口。普通浏览器没有 Tauri 数据库接口。

**没有卡片需要复习？**

卡片可能已经完成今天的复习。可在 **Cards** 查看下次日期；想演示新一轮时，可以创建一张新卡。

**翻面没有译文？**

手动卡或字幕卡需要自行补充译文。词典卡会展示已有英文释义。

**词典查询失败？**

确认网络连接后重试。查询依赖第三方词典服务；已有本地数据不会因此丢失。

**重启后记录还在吗？**

在同一应用标识和本地数据目录下，记录会保留。备份方法和默认路径见 [README](./README.md#数据与限制)。
